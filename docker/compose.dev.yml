services:
  bot:
    build:
      context: ..
      dockerfile: docker/Dockerfile
      target: development
    env_file: ../.env.development
    volumes:
      - ../src:/app/src:ro
      - ../data:/app/data
    restart: unless-stopped
